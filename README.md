# Сравнительный анализ трансформеров и традиционных методов машинного обучения

Проект посвящён сравнительному анализу классических методов машинного обучения и трансформерной модели в задаче многоклассовой классификации текстов на датасете AG News.

Исследование направлено на выявление различий в:

 - качестве классификации
 - структуре ошибок
 - способности учитывать контекст
 - вычислительных затратах

---

## Цель проекта

Провести сравнительный анализ трёх подходов к классификации текстов:

 - TF-IDF + Logistic Regression
 - TF-IDF + Linear SVM
 - DistilBERT (fine-tuning)

Задачи исследования:

 - измерить Accuracy и F1-macro
 - построить confusion matrix для каждой модели
 - проанализировать характер ошибок
 - сравнить вычислительные затраты
 - сделать вывод о практической применимости моделей

---

## Датасет

Используется AG News Classification Dataset.
4 класса новостей:
| Метка | Категория |
| ----- | --------- |
| 0     | World     |
| 1     | Sports    |
| 2     | Business  |
| 3     | Sci/Tech  |

Каждый объект содержит:

 - заголовок
 - краткое описание
 - метку класса

Датасет широко используется как benchmark-задача для NLP-моделей.

---

## Архитектура решений

### Baseline 1 — TF-IDF + Logistic Regression

Параметры:

 - max_features = 50000
 - ngram_range = (1, 2)
 - удаление английских стоп-слов

Особенности:

 - быстрый расчёт
 - линейная модель
 - отсутствие учёта контекста

---

### Baseline 2 — TF-IDF + Linear SVM

Параметры TF-IDF аналогичны предыдущей модели.

Особенности: 

 - более устойчива к шуму
 - немного лучше разделяет классы
 - остаётся лексически-ориентированной моделью

---

### Transformer — DistilBERT

Использована модель: distilbert-base-uncased

Параметры обучения:

 - 1 эпоха
 - learning rate = 2e-5
 - batch size = 32
 - weight decay = 0.01
 - max_length = 96
 - оптимизатор Adam

Обучение выполнялось в Google Colab с использованием GPU.

---

## Результаты

Accuracy

 - TF-IDF + Logistic Regression → ~91–92%
 - TF-IDF + Linear SVM → ~92–93%
 - DistilBERT → ~94%

---

F1-macro

 - DistilBERT → ~0.94

Macro-усреднение использовано для корректного учёта качества по всем классам.

---

## Анализ ошибок

Построены confusion matrix для каждой модели.

Наблюдения:

 - Линейные модели часто путают:
   - World ↔ Sci/Tech
   - Business ↔ Sci/Tech
 - Класс Sports классифицируется наиболее стабильно
 - DistilBERT существенно снижает межклассовую путаницу
 - Оставшиеся ошибки обусловлены семантической близостью тематик

Transformer-модель лучше различает контекстуально похожие тексты.

---

## Сравнение подходов

| Критерий          | TF-IDF модели | DistilBERT |
| ----------------- | ------------- | ---------- |
| Скорость обучения | Очень высокая | Ниже       |
| Требуется GPU     | Нет           | Да         |
| Учёт контекста    | Нет           | Да         |
| Память            | Низкая        | Выше       |
| Итоговое качество | Высокое       | Выше       |

DistilBERT обеспечивает более точную классификацию, но требует больших вычислительных ресурсов.

---

## 🛠 Используемые технологии

 - Python
 - PyTorch
 - HuggingFace Transformers
 - Scikit-learn
 - Pandas
 - NumPy
 - Matplotlib
 - Google Colab

---

## Методология оценки

Использованы метрики:

 - Accuracy
 - F1-macro
 - Confusion Matrix

Валидационная выборка отдельно не выделялась, поскольку целью работы являлся сравнительный анализ моделей, а не подбор гиперпараметров.
Все модели оценивались на одной и той же тестовой выборке.

---

## Запуск проекта

Проект предназначен для запуска в Google Colab или Jupyter Notebook.

Основные шаги:

 1. Установка зависимостей
 2. Загрузка датасета
 3. Обучение baseline-моделей
 4. Fine-tuning DistilBERT
 5. Оценка качества и построение матриц ошибок

---

## Статус проекта

Учебный исследовательский проект в области:

 - NLP
 - сравнительного анализа моделей
 - трансформерных архитектур
 - прикладного машинного обучения

---

